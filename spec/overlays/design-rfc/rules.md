# `design-rfc` scoped rules

**ITWS version:** 0.8.0-draft

This file lists no rule. ITWS 0.8.0-draft scopes no rule to `design-rfc` alone.

Every shared-core rule in Parts 2–8 applies unless its `Profiles` metadata excludes `design-rfc`.
